@AGENTS.md

# Project Instructions

## What this is
ERP for a refurbished-laptop/desktop reseller (Digitalbluez / Techtenth — two buying/payment entities under one business, not separate vendors). Covers purchasing, SKU/inventory, sales, accessories, and repair/replacement/return tracking. Supabase project is currently a **dev/staging copy**, not production — but treat schema changes with the same care as prod (always back up first; see below).

This repo is an **npm-workspaces monorepo**: `apps/erp` (this ERP, unchanged behavior — see `docs/decisions.md`, 2026-07-28) and `apps/web` (the public DigitalBluez e-commerce storefront) share `packages/{shared,ui,db}`. The ERP remains the single source of truth for catalogue/inventory/pricing/sales; the storefront never duplicates that data — see "E-commerce website" below.

## Stack
- Next.js 16.2.1 (App Router, Turbopack), React 19.2, TypeScript
- Supabase (Postgres + Auth), accessed via `@db/db` (`packages/db/src/{browser,server,admin}.ts`) — `apps/erp/lib/supabase/{client,server,service}.ts` are thin re-export shims onto it, kept so the ~100 existing import sites never had to change
- Tailwind CSS v4, shadcn/radix-ui components — shared primitives live in `packages/ui` (`@db/ui/*`), design tokens in `packages/ui/src/tokens.css`
- No test framework configured — verification is via live HTTP scripts against a running `npm run dev` (see Testing below)

## Architecture
- API routes (`app/api/**/route.ts`) do all writes via `supabaseAdmin` (service-role client) — RLS is not the enforcement boundary for most tables; the API layer is.
- Auth/role resolution: `lib/auth/session.ts` (`getSessionUser()` from Bearer token for API routes, `getCookieSessionUser()` for server components), `isOwner()` type-guard.
- Two roles: `owner` and `employee`, stored in `profiles` table. Role check happens in the route handler, not middleware — there is no `middleware.ts`.
- Sensitive-field redaction: `lib/auth/redact.ts` strips cost/vendor/margin fields from API responses for `employee` role. New employee-facing routes should not `.select()` those columns at all rather than fetch-then-redact.
- Page-level gating (`components/RequireOwner.tsx`) is UX-only, not a security boundary — every route it wraps must also check role server-side.
- Generic owner-curated dropdown lists (CPU, RAM, storage, staff names, etc.) live in one `custom_options` table, managed via Settings → Dropdown Options (`components/DropdownOptionsManager.tsx`), read via `lib/useCustomOptions.ts` + `components/SearchableSelect.tsx`. Add new dropdown types by using this pattern, not a new table.

## Data model (see `docs/project-context.md` for full detail)
- **`sku_master` is the single universal catalog for every sellable physical item in the business** — laptops, desktops, monitors, tablets, and every accessory (RAM, SSD, CPU, GPU, keyboard, mouse, and anything else, via the generic `ACC` category). There is no separate catalog/quantity table for any category of item. `sku_category_templates` defines the spec schema per category; adding support for a new *kind* of item means adding a category row there, never a new table. `stock_movements` (trigger-synced into `sku_master.quantity_in_stock`) is the universal quantity ledger for all of them. `asset_ledger` sits on top of `sku_master` only for items that need **per-unit** tracking (serial number, QC, warranty, individual sale) — laptops/desktops/monitors/tablets need this; fungible accessories (a mouse, a stick of RAM) do not and are tracked by quantity alone, the same way `stock_movements` already supports with zero dependency on `asset_ledger`. Before adding any new item type or a new inventory/quantity mechanism, check whether it's just a new `sku_category_templates` category first — this exact drift (a second, undocumented `accessories`/`accessory_movements` table pair growing up alongside `sku_master`) is why this rule is now written down; see `docs/decisions.md` for the consolidation that fixed it.
- `asset_ledger` is the per-unit inventory table (one row per physical laptop/desktop/monitor/tablet) for categories that need per-unit tracking. It has `source` ∈ `('purchase_order','legacy_purchase','employee_intake')` — **never mix these in a query without an explicit reason**; the employee-facing "Live Stock" system and the owner's "main ERP" Stock page are deliberately kept non-overlapping by filtering on `source`.
- `asset_number` is only assigned when a real Purchase Order exists for a unit (via `reserve_assets()` RPC, called from `/api/purchase-orders/from-intake` or the PO wizard). Units can be QC'd, sold, and returned entirely by `serial_number` before they ever get a PO/asset number. Do not add an asset-number requirement anywhere in the operational (QC/sell/service) flow. Fungible/quantity-only SKUs (most accessories) never get asset numbers at all — a purchase for them is a single `purchase_order_items` row with `quantity = N` (no `reserve_assets`, no per-unit `asset_ledger` rows), whether it enters via the normal PO wizard (category-aware `submit`/`receive`, see `lib/sku-categories.ts`) or the deferred `from-accessory-stock` attach. The serialized-vs-fungible branch — not a separate accessory codepath — is what keeps PO/receive/Purchase-Invoice a single flow for every sellable item.
- `sku_master.quantity_in_stock` is a trigger-maintained cache (`trg_sync_sku_stock`) driven off `stock_movements` inserts — never update `quantity_in_stock` directly from application code. This trigger only reads `stock_movements.sku_id`/`quantity_change`; it has no dependency on `asset_ledger`, which is exactly what makes quantity-only (no per-unit row) tracking of accessories work correctly today.
- `sales.sold_by` is a **plain text name** (not a login-account FK) drawn from `custom_options` category `staff_names`, so staff without their own account can still be credited.
- Money fields (`cost_price`, `vendor_id`, `unit_price`, GST cost breakdowns) are redacted from `employee`-role API responses. Selling price is NOT redacted — employees can see and set it.
- **`activities` is the single reusable task/collaboration model.** A module that needs a "someone should do X" item creates an `activities` row (optionally linked to a business record via `related_type`/`related_id`) — never a new per-module task table. The derived operational checklist at `/dashboard/pending-tasks` stays derived (computed live from other tables), never stored, and is not part of the activities system. Notifications for any module go through one generic `notifications` table keyed by `recipient_id` — never a per-module notifier. See `docs/decisions.md` (2026-07-24, Activity Hub redesign, and the Phase 1/2/3 "built" entries) for the full architecture — the redesign is complete: assignment + shared visibility, comments/@mentions, in-app + email notifications, and `pg_cron`-driven due-soon/overdue reminders + attachments + reactions/pinning. `@mentions` are restricted server-side to users who can already see the task (creator/assignee/owner) — never extend a mention to grant implicit access. Cron-driven notifications (`due_soon`/`overdue`) are in-app only, not emailed — see the Phase 3 ADR for why.

## E-commerce website (`apps/web`)
- **The ERP is the single source of truth; the website never gets a copy of catalogue/inventory/pricing data.** It reads through Postgres views (`public_products`, `public_product_images`, `public_categories`) that are owned by a privileged role and select only publish-safe columns — `base_cost`, `cost_price`, `vendor_id`, `gst_percentage` and every other cost/vendor/margin field are never `SELECT`ed by these views, so there is no redaction-in-app-code risk the way `lib/auth/redact.ts` has for staff routes. `anon`/`authenticated` are granted `SELECT` on these views only, **never** on `sku_master`/`asset_ledger`/`product_images` directly.
- **Publishing is owner-curated, opt-in, per SKU**, via a "Website" action on the SKU Master list (`components/SkuWebPublishDialog.tsx`, owner-only) — publish toggle, web price/MRP, slug/title/description/highlights/condition-grade, and a photo manager. `sku_master` columns: `is_published` (default `false`), `web_price` (falls back to `selling_price_default` if null), `market_price` (MRP), `web_slug`, `web_title`, `web_description`, `web_highlights`, `web_condition_grade`, `published_at` (server-set on the publish/unpublish transition, never client-supplied). A SKU is only visible on the website once `is_published = true`.
- **`public_products.availability_bucket`** is computed per the existing serialized/fungible split (`lib/sku-categories.ts`'s `NON_SERIALIZED_CATEGORIES`) — fungible categories bucket off `quantity_in_stock` minus active reservations, serialized categories off a live count of sellable, unreserved `asset_ledger` rows — never an exact number, so the public site never leaks precise stock counts.
- **Product images**: `product_images` table + a public Supabase Storage bucket `product-images`, written only via the ERP's owner-only image routes (`/api/sku-master/[id]/images*`), read publicly via `public_product_images`.
- **Web customer accounts are a distinct identity from staff `profiles`.** `customer_profiles` (PK = `auth.users.id`) links 1:1 to a row in the existing CRM `customers` table (never a second customer concept) so `sales.customer_id` works unchanged once an order becomes a sale. Auth is email+password for now (`POST /api/auth/signup`, `email_confirm: true` since no verified email-sending domain is configured yet — revisit once Resend is live). Cart (`cart_items`) is plain customer-owned CRUD under RLS (`customer_id = auth.uid()`), written client-direct, no API route.
- **An online order is just another sales channel into the ERP.** Checkout (`POST /api/checkout/start`) re-prices from `public_products` (client cart price is never trusted), creates `orders`/`order_items`, then calls the `reserve_order_items` RPC to atomically claim inventory *before* payment — serialized items get one specific `asset_ledger` row locked via `FOR UPDATE SKIP LOCKED` (status → `reserved_web`, a new value alongside every existing `asset_ledger.status`), fungible items get a quantity hold in `web_reservations`, both with a 15-minute TTL released by the `release-expired-web-reservations` pg_cron job (every 5 min). Payment success arrives via the Razorpay webhook (`POST /api/webhooks/razorpay`, signature-verified, idempotent) which converts each `order_item` into a real `sales` row (`apps/web/lib/order-to-sale.ts`) — same `stock_movements`-trigger-driven decrement, same `sold_by='Website'`/`payment_account='Digitalbluez'` convention as an in-store sale, `entered_by` left `NULL` (no staff entered it). `reserve_order_items`/`release_expired_reservations` are `SECURITY DEFINER` but **`EXECUTE` is revoked from `anon`/`authenticated`** — callable only via the service-role client from apps/web's own routes, never directly from a browser (unlike `is_staff()`/`is_owner()`, which must stay anon/authenticated-executable because they run *inside* RLS policy evaluation).
- **`SELLABLE_STATUSES`/`financialYear` live in `@db/shared`**, not duplicated — `apps/erp/lib/sales-entry.ts` re-exports them so its own ~7 existing import sites are unaffected, and `apps/web`'s order-to-sale conversion uses the exact same definitions.
- **Per-unit QC data is already captured and reused, not duplicated.** `asset_qc_checks` (staff-entered via the Stock page's QC screen) is the source of truth for the website's Test Report — a new checklist item is a client-side array edit there, never a new table. Its RLS must stay `is_staff()`-gated (matching `asset_ledger`), since customers and staff share the Supabase `authenticated` role — a permissive policy here isn't just an ERP-internal risk once this data is publicly displayed as a trust signal.
- **Upgrade-option pricing (RAM/SSD/warranty-month upsells) is always admin-configured** via `sku_upgrade_rules` (Settings → Website Admin), never hardcoded in the storefront. A RAM/SSD upgrade is a real paid physical-modification service fulfilled by staff before shipping (routed through the existing `activities` system as a task) — not an automatic swap — because `reserve_order_items` reserves by `sku_id` alone with no spec-awareness, and this business's inventory is one-of-a-kind serialized stock, not bulk variants.
- **Every checkout price input — base price, upgrade deltas, promotion discounts — is re-validated server-side and baked into the final `order_items.unit_price` before any Razorpay call.** Never leave a discount or upgrade cost sitting only in display/audit metadata (`orders.discount_amount`, `selected_upgrades`) while `unit_price` still holds a pre-discount figure — `apps/web/lib/order-to-sale.ts` derives GST purely from `unit_price × quantity` with no other input, so anything not baked in there silently under-reports revenue.
- **A free promotional item (`promotions.promo_type = 'free_gift'`) still moves real inventory** through a normal `order_items` row (`unit_price = 0`, tagged `is_promotional_gift`) — never a side-channel that leaves stock uncounted.

## Business rules that must not be violated
- An employee's stock-in/stock-out entry is **immediately real** (inventory moves at entry time) — there is no owner-approval gate on operational data. The owner's job is deferred bookkeeping (attach a PO, generate an invoice), tracked by **derived flags** (`po_id IS NULL`, `sales.finalized`), never a separate status column that can drift out of sync.
- Employees must never see purchase cost, vendor identity, or margin anywhere in the UI or API response. **Exception (2026-08-24, accessories only):** an accessory's *purchase-entry* vendor + unit price — captured optionally by whoever receives the stock (`stock_movements.vendor_id`/`unit_price`, from the same `vendors` table used everywhere else) — is visible to every role by deliberate owner decision. This is distinct from, and does not relax, the owner-only *formal* PO-attach vendor/cost (`purchase_order_items`/`purchase_orders`) for accessories, nor anything vendor/cost-related for laptops/desktops/monitors/tablets, which stay owner-only exactly as before. The employee-visible vendor list itself is also scoped, not just redacted: `vendors.supplies_accessories` (owner-set, default `false`) gates which rows `GET /api/vendors` returns to a non-owner at all, so a laptop-only vendor is never exposed to an employee even by name. Employees can also create a new vendor from the Receive Stock flow (`POST /api/vendors`, full fields, resolved-by-name against any existing vendor rather than risking a duplicate) — always forced `supplies_accessories=true` server-side, immediately real, no approval gate — but still cannot edit, delete, or tag/untag an existing vendor; that stays exclusively on the owner-only Vendors page.
- Only the owner role can: attach units to a PO, generate invoices, edit SKU master data, manage vendors (edit/delete/tag any existing vendor — creating a new accessory-tagged vendor from Receive Stock is the one narrow employee exception above), manage dropdown-option lists. **Repair job** fields (`status`, and — as of 2026-08-04 — `payment_status`/`amount_paid`/`payment_account`) are gated by the `repair_jobs` page edit grant (`canEditPage`), not owner-only; marking a job "Done" also creates a linked `sales` row (`sales.repair_job_id`) so the charge appears in the Sales Ledger and can be combined into a GST invoice via the existing multi-item invoice flow, same as any other sale.
- **Sale payments are an append-only ledger, not an owner-only field.** `sales.amount_paid`/`payment_status` are trigger-derived (`sync_sale_payment_totals`) from the sum of `sale_payments` rows — any role can record a new installment via `POST /api/sales/[id]/payments` (an employee taking a customer's 2nd/3rd payment logs it themselves, same "immediately real" principle as the sale itself), matching the sale's own display on Live Stock/Main ERP Stock's Sold tab. Only the owner can remove/correct an erroneous entry (`DELETE /api/sales/[id]/payments/[paymentId]`) or edit `payment_account`. Never write `amount_paid`/`payment_status` directly from application code — add a `sale_payments` row instead.
- Numbering (asset numbers, PO numbers, invoice numbers, repair job numbers) must go through the existing atomic RPCs (`reserve_assets`, `generate_po_number`, `increment_invoice_number`, `generate_repair_job_number`) — never a client-side MAX-scan or manual counter increment.
- Tasks/activities never carry or surface cost price, vendor identity, or margin. If a task's UI previews a linked business record, it must fetch that preview through the existing redacted endpoints (e.g. `/api/stock`), never a fresh cost-bearing query.

## Coding conventions
- Reuse existing lib helpers before writing new logic: `lib/sku-resolver.ts` (`resolveOrCreateSku`), `lib/purchase-utils.ts` (`recalcPOTotals`, `getVendorName`), `lib/accessory-movements.ts`, `lib/sales-entry.ts`.
- Prefer extending a shared component over forking a near-duplicate page (e.g. `components/StockView.tsx` is parameterized and mounted at two routes rather than copy-pasted).
- No test framework — verify behavior with a disposable Node script that signs in real Supabase auth users (owner + employee), hits the running dev server's real HTTP endpoints, and **always cleans up every row/user it created before finishing** (verify this actually happened — don't trust a script's own "cleaned up" print statement without re-querying).
- Don't run `rm -rf .next` while `npm run dev` is running — it corrupts the running dev server; stop it first if you need a clean build.

## UI / Design system
- Page container spacing, mobile top-bar clearance, and the ultrawide max-width cap live once in `app/dashboard/layout.tsx` (`p-4 md:p-6 max-w-screen-2xl mx-auto`, `pt-14 md:pt-0`) — don't re-add page-level padding in individual pages beyond a narrower content `max-w-*` where a form intentionally isn't full-width.
- Reuse shared building blocks before writing a new one: `components/ui/*` (shadcn primitives — Button/Input/Select/Checkbox/Dialog/Table/Badge all share an `h-8` control-height convention), `components/Pagination.tsx`, `components/EmptyTableRow.tsx`, `components/ErrorBanner.tsx`, `components/StatusBadge.tsx` + `lib/status-styles.ts` (per-domain status/priority → color tone maps), `components/SimpleModal.tsx` (or shadcn `Dialog` with `max-w-* max-h-[90vh] overflow-y-auto`) for any new modal, `components/AsyncCombobox.tsx` (shared scaffold behind `SearchableCustomerSelect`/`SearchableItemSelect`) or `components/SearchableSelect.tsx` (sync local-list + free-text fallback) for any new searchable dropdown.
- Every checkbox uses `components/ui/checkbox.tsx` (Radix-based, supports `checked="indeterminate"` for header select-all) — never a raw `<input type="checkbox">`.
- List/table pages that can grow large paginate server-side: API routes accept optional `page`/`limit` (via `lib/pagination.ts`'s `parsePagination()`) and return `{ data, total }` only when `page` is passed — omitting it keeps the old unbounded-array response, so existing non-paginated callers (search widgets, RMA pickers, etc.) never break. Client-direct-Supabase pages (Customers/Vendors/Invoices) use `.range()` the same way. A table's own `#` row-index column (when present) is always `(page-1)*pageSize + rowIndex + 1` — never conflate it with a business identifier (asset number, invoice number, PO number, job number), which stays its own column.
- **Every list/table view's date column goes first among the data columns** (immediately after the `#` row-index column when one exists, otherwise as the literal first column) **and the view default-sorts descending by that date** (most recent first) on load. Applies to every current list page (Sales Ledger, Repair Jobs, Purchase Orders, Purchase Invoices, Quotations, RMA, Invoices, Purchases ledger, Activities, Stock/Live Stock) and every future one. If a page fetches via direct Supabase query or a dedicated API route, the default `.order(...)` must match the displayed date column, not an unrelated timestamp like `created_at` — add a secondary `created_at desc` tiebreak when the date field isn't unique per row. Card/mobile variants (`md:hidden`) follow the same rule: date shown first in the card. Exception: card headers in a repair/job-style card that need the record number as the visual anchor may show date as a small line above the number rather than literally the first DOM node, as long as it reads first.
- Currency/numeric table columns are `text-right tabular-nums`; every table wraps in `overflow-x-auto` inside a `rounded-md border` container.
- A page whose primary table is realistically checked from a phone in the field (Live Stock, Repair Jobs) gets a real `md:hidden` card-per-row layout alongside the `hidden md:block` table, sharing the same row component/state (see `JobRow`'s `variant` prop in `app/dashboard/repair-jobs/page.tsx`) rather than duplicating logic — don't build a second, separately-maintained mobile-only component.

## Before modifying code
- Check for an existing table/column/RPC/helper before adding a new one — this schema has accumulated some duplicate/dead mechanisms in the past (see `docs/decisions.md` for what was already cleaned up and why).
- Check `docs/current-progress.md` for what's already built vs. in progress before starting new work in the Stock/Sales/Repair/Accessories area.

## Autonomous development rules
- Always back up before a schema migration: `supabase db dump --linked -f backups/<date>_<label>_schema_backup.sql` (schema) and `--data-only` (data).
- Apply migrations via the Supabase MCP tools, not raw psql.
- Run `mcp__supabase__get_advisors` after schema changes.
- Type-check and build per-app now that this is a monorepo: `cd apps/erp && npx tsc --noEmit -p . && npx next build` (same for `apps/web`), or `npm run typecheck`/`npm run build` at the repo root to run both via Turborepo. Always with the dev server stopped.
- Don't ask "should I proceed?" once a plan is approved — proceed and only pause for destructive/irreversible actions or a decision only the user can make.
